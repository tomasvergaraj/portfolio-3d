node_modules
dist
dist-ssr
screenshots
tmp
character
# Los modelos 3D de public/ SÍ se versionan: el deploy (Vercel) los necesita,
# si no, el mundo cae a primitivas. Los GLB ya van comprimidos con meshopt; los
# FBX del avatar son pesados (~45 MB) — pendiente convertir a GLB para aligerar.
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
